1.用来求分数的上界
  int val = (p+m-1)/m

2.特殊的取模运算
  比如模为14，我们要让取模运算后的结果分布在1-14之间，也就是不包含0，那么如果是14，原本取模运算为0，而现在应该为14
  写法为：(N-1) % 14 + 1

3.binary search小技巧
  在binary search中经常要计算mid,通常mid的计算如下：
  int mid = lo+(hi-lo)/2;
  但是，这样计算，算出来的是一个下界，比如lo为3，hi为4，那算出来mid为3。
  那如果我们想要计算出的结果为上界，也就是让结果为4怎么办？，可以如下计算：
  int mid = lo+(hi-lo+1)/2;
